import {h} from "../../component/basic_unit/element";
import {CSS_PREFIX} from "../../global_utils/config_for_core_and_component";

export class ChartView {
    constructor() {
        this.el = h('div', `${CSS_PREFIX}-chart`);
    }


}
